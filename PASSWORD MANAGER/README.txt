The application allows the following functionality:

1. Create a new key
2. Create a new password storage file with the created key
3. Open an existing password file and read it with the associated key
4. Find a specific password from the file by entering the application name it is stored under
4. Add a new password to a password file

If the key for a password file is lost the application cannot decrypt it. 
